# HFADM 代码理解记忆

> 本文件由源码通读压缩而成，作为项目长效记忆（源码才是真相来源）。
> 更新：2026-08-10（第五轮：远程协议 v2 功能增强——写操作幂等键、RemoteClient 全面异步化+旧API桥接、mainwindow 11 处调用点异步迁移、搜索三关键词、图号点击打开最新版图纸）。
> 注：tests/ 目录已不存在（2026-08-06 远程历史重写后未恢复），无自动化测试，验证靠编译通过 + 手动运行。
> 源码规模：约 82 个源文件（根层 4 + model 7 + database 4 + service 9 对 + ui 25 对 + assets 13）。

---

## 1. 项目概述

- **HFADM** = 无人机硬件装配数据管理系统（个人学习项目，非商业）。
- 用途：以「机型 → 部件 → 零件」产品结构树组织装配数据，零件可挂多版 PDF 图纸；支持**物理删除（无回收站、不可恢复）**、多项目共存、会话恢复、备份、**局域网远程访问（v2：配对口令 + AES-128-GCM 加密 + 设备授权管理）**。
- 形态：Windows 桌面应用（Qt Widgets），一项目 = 一个 `hfadm.db` + `files/` 目录（PDF 平铺）。
- 设计理念：DB 管关系、文件管文件；**删除即物理删除**（node/drawing 表 `deleted` 字段保留但恒 0，兼容旧软删残留，不再置 1）；界面像资源管理器（目录树 + 详情表 + 多标签）。

## 2. 技术栈与构建

- Qt 6.11.1 / Widgets / C++17 / CMake / MinGW 13.1 / SQLite / Qt PDF / QtSvg / **Qt Network** / **Windows BCrypt(CNG)**。
- Qt 模块（CMakeLists `find_package`）：`Core Widgets Sql Pdf PdfWidgets LinguistTools Svg Network`。
- 加密：AES-128-GCM（16字节key/12字节nonce/16字节tag），Windows BCrypt/CNG 原生实现（`service/crypto`），`#pragma comment(lib,"bcrypt.lib")` 链接；无第三方依赖。非 Windows 平台 crypto 为兜底空实现（项目仅面向 Windows）。
- 配置文件：**hfadm.session（程序同目录 INI，即软件配置文件）**，`service/appconfig` 统一读写——客户端凭证 `remote/credentials/<host>`、服务端拒绝黑名单 `remote/ignoredDevices`、本机设备身份 `remote/deviceUuid`。**ClientCredentialStore 已从注册表迁移到 hfadm.session**；`SessionManager` 只管理自己的键（tab*/activeIndex/count + detailView 列宽），**不再 clear 整个文件**。
- 构建：CMake + Ninja（`cmake -S . -B build/main -G Ninja`，编译器须用绝对路径 g++）；统一构建目录 `build/main`（主程序）+ `build/tests`（测试，`-S tests -B build/tests`），根目录下不再有其他构建目录（2026-08-06 整理）。**坑：构建/运行测试时需 `PATH=/c/Qt/Tools/mingw1310_64/bin:/c/Qt/6.11.1/mingw_64/bin:$PATH`**（windres/链接/DLL 加载需要，否则 `windres: fatal: CreateProcess failed`）。
- 翻译：`qt_add_translations(HFADM_zh_CN.ts)`，资源 `:/i18n/`，main.cpp 按系统 locale 加载。
- 资源：两个 `qt_add_resources`——`welcome_assets`（`:/assets/welcome/`：欢迎页背景 jpg + 3 按钮 SVG）、`toolbar_icons`（`:/assets/Icons/`：后退/前进/上/刷新/主页/新建 + icon.svg）。
- 图标：`main.cpp` `a.setWindowIcon(QIcon(":/assets/Icons/icon.svg"))`（QApplication 级，所有窗口继承）；exe 文件图标 = `icon.rc.in` → `configure_file` 注入绝对路径 → `$<$<BOOL:${WIN32}>:${BINARY_DIR}/icon.rc>` 由 windres 编译进 exe。**坑：icon.rc.in 被误删会导致 cmake configure_file 失败（必要时 git checkout 恢复，已发生一次）。**
- 发布：~~`scripts/deploy.bat`~~（2026-08-05 已删除，不再同步）。发布 = Release 构建后手动 `windeployqt --release --no-translations --no-system-d3d-compiler <exe>` 到 `dist\HFADM`；`QT_BIN=C:\Qt\6.11.1\mingw_64\bin`；windeployqt 路径须 Windows 风格 `C:/...`（bash 的 /c/ 会被拒）。产物约 43MB。
- 测试：~~tests/ 已删除（2026-08-06 远程历史重写后 gitignore+目录消失，原 test_core 71 通过 / test_remote 回环 failures=0 均已不存在）~~。验证 = `cmake --build build-release -j2` 编译通过 + PowerShell 启动进程存活 + 手动跑流程。

## 3. 分层架构（UI → Service → Model → Database）

```
MainWindow(ui/* 组件)
   ├─ Service: ProjectService(拥有唯一 DatabaseManager) · NodeService · DrawingService
   │              · RemoteServer(开放方，监听312) / RemoteClient(连接方，业务API异步+旧同步桥接，awaitOnce模式)
   │                   ├─ 加密层: service/crypto(AES-128-GCM/BCrypt) + appconfig(统一配置/凭证/黑名单/本机uuid)
   │                   └─ 远程读写复用 NodeService/DrawingService，走 JSON 行协议(v2 加密帧)
   └─ Database: DatabaseManager(统一 SQL) + DrawingRepository(drawing 专属 SQL)
        └─ SQLite: <project>/hfadm.db (WAL, foreign_keys=ON) + files/  + remote_device 表(机型库内)
   └─ Model: 纯数据结构 (hfdadnode/projectinfo/part/component/drawing/deletionplan/remotedevice)
```

- 所有 SQL 经 `DatabaseManager`（drawing 下放到 `DrawingRepository`）；浏览用 `QTableView + QAbstractTableModel`（禁 `QTableWidget`）。
- **多项目共存**：TabData 记 `projectPath`；切标签 `activateProjectContext()` 重开对应项目库（同一 DatabaseManager 复用、指向当前库），即同一时刻仅一个库打开。标签顶色带 6 色板区分归属。
- **远程上下文切换**：`RemoteServer::withProjectContext()` 处理每请求前 `openProject(绑定项目)` 切上下文、完事恢复原激活项目；单线程事件循环下切换安全。**v2 起每个业务请求额外加解密（AAD 绑定帧 id）**。
- **删除链路**：确认弹窗统计（`collectDeletionPlan`）→ `deleteNodeTree` 事务内物理删记录 → `NodeService::deleteNode` 提交后清理磁盘文件；UI 经 `ui/deleteprogressdialog` 逐项汇报。

## 4. 目录结构总览

```
CMakeLists.txt / main.cpp / mainwindow.{cpp,h,ui} / HFADM_zh_CN.ts
model/     hfdadnode.h  projectinfo.h  part.h  component.h  drawing.h  deletionplan.h  remotedevice.h   (纯结构, header-only)
database/  databasemanager.{h,cpp}  drawingrepository.{h,cpp}
service/   projectservice / nodeservice / drawingservice / remoteprotocol / remoteserver / remoteclient
           / crypto（AES-128-GCM/BCrypt） / appconfig（统一配置：凭证/黑名单/本机uuid） / clientcredentialstore（客户端凭证，代理 appconfig）（9 对）
ui/        nodetablemodel · pdftabviewer · dialogs · recentprojectsmenu · tabmanager · browsertabbar
           · browsertabwidget · sessionmanager · welcomepage · contextmenus · directorynavigator
           · directoryassembler · clipboard · drawingoperations · nodeoperations
           · parteditordialog · pdfpreviewdialog · remotedialog · remoteoperations · pinpromptdialog
           · pinlabel · deleteprogressdialog · remoteparteditordialog · pairingdialog · devicemanagerdialog（25 对）
tests/     test_core.cpp  test_remote.cpp  CMakeLists.txt（本地测试，git 已忽略）
assets/    欢迎页 jpg + 3 svg（welcome-page-*）；Icons/ 工具栏 7 svg + icon.svg + icon.ico + icon.rc.in
```

## 5. 数据库 Schema（DatabaseManager::createTables）

库文件 `<project>/hfadm.db`；图纸 `<project>/files/`；`PRAGMA foreign_keys=ON; journal_mode=WAL;`；备份前 `wal_checkpoint(TRUNCATE)`。

- **project**：`id/name/version('1.0')/create_time/update_time/last_open_time`，单条记录（`WHERE id=1`）。
- **node**（自关联结构树）：`id/parent_id(FK,根=NULL)/name/type(1机型2部件3零件)/part_no/create_time/update_time/deleted`。**`deleted` 字段保留（DEFAULT 0，恒 0，不再置 1；兼容旧软删残留，查询仍过滤 deleted=0）**。索引：`idx_node_parent`、`idx_node_name`、部件段全机型唯一部分索引 `idx_node_part_no_component ON node(part_no) WHERE type=2 AND part_no IS NOT NULL`、零件段同父唯一 `idx_node_part_no_part ON node(parent_id,part_no) WHERE type=3`。
- **part**（零件属性）：`id/node_id(FK)/material/quantity(默认1)`。
- **component**（部件属性）：`id/node_id(FK)/quantity(默认1)`。
- **drawing**（图纸版本）：`id/part_id(FK)/file_name(如 AHZ700.3010.10A_球头.pdf)/file_path/files下相对/version(字母A…)/is_current/create_time/deleted`（deleted 同 node 保留恒 0）。索引 `idx_drawing_part`。
- **operation_log**（审计）：`id/operation/target_id/target_type(节点用NodeType值,图纸用10)/create_time`，写失败不影响主流程。
- **remote_device**（远程设备记录，v2 新增，建在机型项目库内，CREATE IF NOT EXISTS 自动迁移旧库）：`uuid(PK)/device_name/aes_key(Base64)/permission(0受限 1完全 2管理遗留 3不允许=黑名单)/created_at/last_seen`。首次配对成功后写入；二次连接按 uuid 查密钥与权限做挑战-响应认证；**黑名单（remote/ignoredDevices，hfadm.session）是行为真相源，表内权限只是同步镜像**。
- **remote_idempotency**（写操作幂等去重表，2026-08-10 新增）：`device_uuid/id_key(复合PK)/response_json/created_at`。内容哈希幂等：客户端对写命令自动算键（参数排序 JSON + `_cmd` → SHA-256 hex）随加密请求发送，服务端 `handleBusiness` 写命令入口查表——命中则原样返回首次响应不重复执行，未命中执行后在事务内记录 `{success,data/message}`。覆盖全部 12 个写命令（含 importPdf：文件 base64 参与哈希，同一文件重传不重复生成图纸版本）。清理：`cleanupExpiredIdempotency(keepDays)` 按 created_at 删除。
- 关系链：`node(机型)→node(部件)→node(零件)→part/component→drawing`。**删除**：`collectDeletionPlan`（后序收集子树，叶子优先；子节点查询**不过滤 deleted** 以含旧软删残留，保证删除彻底）→ `deleteNodeTree`（事务内逐节点删 drawing→part→component→node，每删一节点回调 onNodeDeleted）。

## 6. 各代码文件详解

### Root 层
- **CMakeLists.txt**：HFADM 目标含 25 组 ui + 9 组 service（含 v2 新增 crypto/appconfig/clientcredentialstore/pinlabel/pinpromptdialog/pairingdialog/devicemanagerdialog）；资源双前缀（welcome_assets / toolbar_icons）；exe 图标链路（icon.rc.in→configure_file→`$<$<BOOL:${WIN32}>:...>`）；`install` + `qt_generate_deploy_app_script`。疏漏：`model/component.h`、`model/deletionplan.h` 未列入主目标源清单（均 header-only 无碍）。
- **main.cpp**：建 QApplication、`setWindowIcon(:/assets/Icons/icon.svg)`、按 locale 加载 `:/i18n/HFADM_<locale>`、显示 MainWindow。
- **mainwindow.h/.cpp**：主窗口 Facade。成员：3 Service + RemoteServer* + 剪贴板来源 `RemoteClient* m_clipboardClient` + 状态栏远程标签 `m_remoteStatusLabel` + 列宽防抖定时器 `m_columnWidthSaveTimer`。菜单：文件/编辑/**网络**/帮助；网络菜单=「开放远程访问」「关闭所有连接」「**授权管理(&M)**」（开放/关闭互斥置灰；授权管理在开放状态下可用，弹 DeviceManagerDialog）。关键方法：`initServices`、`createMenus`（代码建菜单栏，不挂 QMainWindow 顶部）、`loadCurrentDirectory`（远程分支走 RemoteClient）、`updateActionState`（含远程 tab 判断：远程目录/远程PDF 标签均 `isRemoteTab()`，网络 action 全灰）、`isRemoteTab()/currentRemoteClient()`、`onOpenRemoteAccess/onCloseRemoteAccess/onConnectRemote`、`updateRemoteStatusBar`（常驻「IP:312 · N连接」）。**配对解析器**：`m_remoteServer->setPairingResolver(λ)` 在构造时设为弹窗（`showPairingDialog(this, req, server)`）；测试场景可替换为自动批准。**删除流程 `deleteSelectedNode`**：①`collectDeletionPlan` 汇总统计（多选合并去重）②确认弹窗「将删除其下全部 N 个节点、M 张图纸，此操作不可恢复！」③模态 `DeleteProgressDialog`（setTotal → show + processEvents 驱动 → 逐节点/逐文件 advance+addLog → 完成或失败后 exec() 等用户点关闭）。远程操作槽在重命名/删除/属性/剪贴板/图纸各槽首部分支 `isRemoteTab()`。**2026-08-10 起远程分支不再调 remoteoperations 同步函数，改为内联异步**：统一模式 `QPointer<RemoteClient> guard + awaitOnce(client,reqId,this,lambda)`，回调内三重校验（guard 存活 / currentTab 仍是发起 tab / currentNodeId 未变）防悬空。已迁移 11 处：loadCurrentDirectory（listDirAsync/searchAsync）、navigateUp（getNodeAsync）、updateNavigationState（远程 canUp 改 `currentNodeId != rootNodeId()` 免查询）、图纸 4 处（importPdfToSelectedPart/viewSelectedDrawing/setCurrentSelectedDrawing/deleteSelectedDrawing）、renameSelectedNode、deleteSelectedNode（`shared_ptr<std::function>` 递归串行 deleteNodeAsync）、showPropertiesDialog（`shared_ptr<PropsState>` + std::function 嵌套：loadPart→loadComponent→computeFullPartNo→弹框→rename/updatePartNo/updatePartAttributes/updateComponentQuantity 串行）、onPasteAction 剪切（递归 moveNodeAsync）。**复制粘贴「复制」冲突循环（remoteCopyWithConflictResolution）因多层循环+弹窗+串行远程异步化复杂，仍走旧同步桥接**。**图号点击打开最新版图纸（2026-08-10）**：detailView clicked 连 `onPartNoClicked`（仅图号列 ColPartNo=2 且零件行）→ `openLatestDrawingForPart`——本地 queryDrawings 找 isCurrent（否则最新）→ resolveDrawingPath 复制到 `m_pdfCacheDir`(QTemporaryDir)；远程 listDirAsync 找图纸 → fetchDrawingFileAsync → 复制缓存 → `openCachedPdf`（QDesktopServices::openUrl 系统默认程序打开；程序退出时 QTemporaryDir 自动清理缓存）。剪贴板跨域：`m_clipboardClient` 记来源（本地=nullptr），粘贴时 `m_clipboardClient != 目标 remoteClient` 拒绝。**详情列宽记忆**：`saveColumnWidths/restoreColumnWidths`——`sectionResized` 触发 400ms 单发定时器防抖写盘（hfadm.session `detailView` 组），closeEvent 兜底保存；恢复放构造最后（restoreSession 之后），默认列宽 {320,120,200,90,170}。**修复（2026-08-06）**：①`restoreSession` 单标签恢复时 `setCurrentIndex` 不触发 `currentChanged` → 显式 `activateProjectContext(t->projectPath)` 保证标题/上下文缓存就绪；②`setProjectOpenState` 在 m_projectOpen 变更后立即 `updateActionState()`（否则首次右键仍是旧动作状态）。生命周期：关闭被开放机型标签 → 自动 stop；远程连接断线 → 关闭该连接全部标签 + deleteLater；saveSession 跳过远程标签。**无回收站菜单/操作**。
- **mainwindow.ui**：centralwidget 垂直布局：①BrowserTabWidget ②toolbarWidget(QMenuBar+分隔线+5 QToolButton) ③searchWidget ④detailView(QTableView)。

### model/（纯结构，header-only）
- **hfdadnode.h**：`enum class NodeType{Aircraft=1,Component=2,Part=3}`；`struct HFADMNode{id,parentId,name,type,partNo,createTime,updateTime,deleted}`（根 parentId=0，deleted 保留恒 false）；`nodeTypeDisplayName()`。注释强调「开发规范 §10：禁止多树模型」。
- **deletionplan.h**：删除计划结构——`QVector<Node> nodes`（后序排列，含直接挂载图纸）+ `drawingCount()`（图纸记录总数）+ `fileCount()`（磁盘文件相对路径去重计数）；供确认弹窗统计与进度弹窗逐项汇报。
- **remotedevice.h**：远程设备记录（v2 新增）——`uuid/deviceName/aesKey(16字节)/permission(RemoteProtocol::Permission)/createdAt/lastSeen` + `ignoredOnly`（仅黑名单设备标记：配对被拒、无表记录，列表由黑名单合成显示，重命名等操作不可用）。服务端持久化于机型项目库 remote_device 表。
- **projectinfo.h / part.h / component.h / drawing.h**：对应各表结构；drawing 含 `partNodeId`，文件名 `{完整图号}{字母}_{零件名}.pdf`。

### database/
- **databasemanager.h/.cpp**（约 1100 行，略超上限）：SQL 统一入口。`openDatabase`(连接名 HFADM_CONNECTION/QSQLITE)→`applyPragmaSettings`(FK+WAL)、`initializeDatabase→createTables`、事务 `begin/commit/rollback`、`checkpointWal(TRUNCATE)`。project 3 方法；node：`insertNode`(根存NULL)/`queryChildren`(根 `parent_id IS NULL` ORDER BY type,name)/`getNode`/`updateNodeName`/`updateNodePartNo`/`updateNodeParent`/`isPartNoTaken`/`searchNodesRecursive`（**2026-08-10 起三关键词：`LEFT JOIN part ON part.node_id=n.id` + `AND (n.name LIKE ? OR n.part_no LIKE ? OR part.material LIKE ?)`，同一 pattern 绑定 3 次，名称/图号/材质；本地 assembleSearchResults 与远程 search 命令共用此函数**）/`searchDrawingsRecursive`（图纸文件名，保留）/`toLikePattern`(转义)。**删除（物理删除，无回收站）**：`collectDeletionPlan`（后序收集子树+直接挂载图纸，静态辅助 `collectDeletionRecursive` 子节点查询不过滤 deleted 以含旧软删残留）+ `deleteNodeTree`（事务内逐节点 DELETE drawing(part_id IN)→part→component→node，每删一节点回调 onNodeDeleted；filePathsToRemove 输出去重后的磁盘文件相对路径）。part/component 各 4 方法。remote_device：`listRemoteDevices/insertRemoteDevice/updateRemoteDevicePermission/remoteDeviceExists/deleteRemoteDevice`（**表内权限只是黑名单的同步镜像，改权限时先写黑名单再写表**）。remote_idempotency（2026-08-10）：`getIdempotencyResult(deviceUuid,idKey,payload)`（查首次响应）/`insertIdempotencyResult`（INSERT OR IGNORE 保首次）/`cleanupExpiredIdempotency(keepDays)`。**已移除回收站四方法（softDelete/permanentDelete/恢复等）**。
- **drawingrepository.h/.cpp**：`insertDrawing`(事务：旧版 is_current=0→新=1)、`queryDrawingsByPartNode`(JOIN part, deleted=0, ORDER BY create_time DESC)、`queryDrawingById`、`setCurrentDrawing`、`getNextVersionLetter`(含已删最大字母 ASCII+1)、`permanentDeleteDrawing`、`updateDrawingVersion`(删后重排)。**已移除 softDelete/restore（回收站版本控制）**。

### service/
- **projectservice.h/.cpp**：项目管理，拥有唯一 DatabaseManager。`createProject`(建目录/拒重复/files/→开库初始化→insertProject→建机型根)、`openProject`(校验+更新 last_open_time)、`isProjectValid`(需 db+files 同存)、`backupProject`(checkpoint→复制带时间戳目录)。
- **nodeservice.h/.cpp**：节点业务与图号体系。`loadDirectory`、`createComponent`(段0–9999纯数字全机型唯一)、`createPart`(父必须部件)、`renameNode/updateNodePartNo`(机型不可改)、**`collectDeletionPlan`（收集后填充完整图号）`deleteNode`（物理删除：`deleteNodeTree` 事务删记录 → 提交后按 `m_projectPath` 解析相对路径 `QFile::remove` 清理磁盘文件，文件删除失败不影响数据一致性；回调 onNodeDeleted/onFileDeleted 供进度弹窗）**、`searchRecursive`(图纸搜索不匹配节点下图纸，drawings 恒空按需求)、`getNodePath`、`moveNode`(防环+零件只进部件)、`copyNode`(事务深拷，图纸不复制)+`findFreePartNo`。图号：`computeFullPartNo` 动态拼接——机型=名称；部件=机型名.段；零件=父完整图号.段；**子部件不继承父段**；机型改名自动刷新。
- **drawingservice.h/.cpp**：图纸导入/版本/命名。`importPdf`(仅PDF；getNextVersionLetter；`{完整图号}{字母}_{零件名}.pdf`；复制 files/；失败回滚删文件)、**`removeDrawing(drawingId, mode)` 物理删除（无回收站；`DrawingRemovalMode{KeepVersions,Renumber}`——KeepVersions 仅删、版本字母空缺保留，Renumber 前移并重命名磁盘文件；删当前且有剩余→最新设当前）**、`setCurrentDrawing`、`static resolveDrawingPath`、`buildRenamedDrawingInfo`(取第一个 `_` 前字母段)。**已移除回收站三方法**。
- **appconfig.h/.cpp**（v2 新增，2026-08-06）：**应用配置统一入口**——`filePath()` 程序同目录/hfadm.session（INI/QSettings）；`deviceUuid()` 本机设备身份（首次生成持久化，配对/认证始终复用，保证被拉黑后重试仍命中黑名单）；客户端凭证 `loadCredential/saveCredential/clearCredential`（键 `remote/credentials/<host>`，ClientCredentialStore 为其薄代理）；服务端黑名单 `ignoredDevices/isIgnoredDevice/addIgnoredDevice/removeIgnoredDevice`（键 `remote/ignoredDevices`）。**坑：空 QStringList 写 INI 会变成 @Invalid()，清空黑名单用 settings.remove。**
- **crypto.h/.cpp**（v2 新增）：AES-128-GCM 加解密（Windows BCrypt/CNG，无第三方依赖）。`generateKey()`(16字节)/`generateNonce()`(12字节)/`generatePin()`(4位数字口令)；`encrypt(key,nonce,plaintext,aad,ciphertext,tag)`/`decrypt(...)`（tag 校验失败返回 false）。CMake 链接 `bcrypt`；非 Windows 为兜底空实现。
- **clientcredentialstore.h/.cpp**（v2 新增）：客户端按服务端地址持久化配对凭证 `{uuid,aesKey,deviceName}`；**2026-08-06 已从注册表迁移为 appconfig 的薄代理**（写 hfadm.session `remote/credentials/<host>`）；`load/save/clear`。认证失败（非「禁止连接」）/被撤销授权时清除回退配对。
- **remoteprotocol.h/.cpp**：远程协议 **v2**（2026-08-05 重构，v1 明文废弃）。**帧格式：单行 JSON + '\n'**。
  - **控制帧（明文，仅握手）**：请求 `{"type":"request","cmd":"connect|auth|authResp|cancelPair","id":N,"body":{...}}` / 响应 `{"type":"response","id":N,"success":bool,"body":{...}}`。命令常量 `kCmdConnect/kCmdAuth/kCmdAuthResp/kCmdCancelPair`（cancelPair=客户端取消配对，关闭服务端挂起的确认弹窗，双方不记录）。
  - **业务帧（加密）**：请求 `{"type":"request","cmd":"<业务>","id":N,"body":{nonce,ciphertext,tag}}`（body 解密得业务参数）；响应 `{"type":"response","id":N,"body":{nonce,ciphertext,tag}}`（解密得 `{success,data,message}`）。cmd 在 envelope 明文，success/data/message 在密文内。**防重放：id 严格递增，AAD 绑定 id**（`encryptBody/decryptBody(key,id,payload)`）。
  - 常量：`kPort=312`、`kProtocolVersion=2`、`kRequestTimeoutMs=5000`（业务）、`kConnectTimeoutMs=60000`（连接/握手，与配对口令 60s 有效一致）、`kHeartbeatIntervalMs=15000`、`kSilenceTimeoutMs=60000`。
  - 权限：`enum class Permission{ReadOnly=0,ReadWrite=1,Admin=2,Denied=3}`——**Denied=黑名单（不允许的连接），设备管理可改入/解除；Admin 为旧版本遗留（行为等价 ReadWrite，新 UI 不再提供，仅兼容历史记录）**；`isDenied(p)`；`permissionToString`：受限的访问权限/完全访问权限/完全访问权限(Admin 兼容)/不允许的连接；`permissionFromString` 兼容新旧字符串（含「可写/ReadWrite/管理员/Admin」）；`isWriteCommand(cmd)` 供只读拦截；**`computeIdempotencyKey(cmd,params)`（2026-08-10）：params 按键排序 JSON（QJsonDocument::toJson 自动排序）加 `_cmd` 字段 → SHA-256 hex，供写操作幂等去重**。
  - 配对结构：`PairingRequest{uuid,deviceName,pin,peerAddress}` / `PairingResult{accepted,permission,neverAskAgain}`。
  - 22 种业务命令常量（）。**deleteNode/deleteDrawing 均为物理删除（不可恢复）**。序列化：node/drawing/part/component/directoryItem/时间(ISODate)。所有读写由服务端（开放方）执行，客户端只收发。
- **remoteserver.h/.cpp**：服务端。`start(projectPath,projectName)` 绑定机型 + 查机型根 id 存 `m_boundRootNodeId`（握手成功响应返回给客户端）；`QTcpServer` 监听 `AnyIPv4:312`，失败报错；`stop()` 关监听+断开全部连接。**坑（已修）：`stop()` 不能先 `delete m_server`（会连带删除 nextPendingConnection 的 socket 导致 RemoteConnection.m_socket 悬空崩溃）；修复为构造函数 `m_socket->setParent(this)` 接管生命周期，析构不再手动删 socket。** 连接流程（状态机：握手→加密）：首次 `connect`（配对）经 `setPairingResolver(std::function)` 回调（MainWindow 设为弹窗、测试自动批准）→ 存 remote_device 表；二次 `auth{uuid}` → 服务端查设备发挑战 nonce → `authResp`（客户端用密钥加密 nonce 回）→ 验证通过建会话；认证失败清凭证回退配对。**黑名单三层拒绝（2026-08-06，黑名单为行为真相源）**：①配对请求：黑名单 uuid 直接拒绝（「该设备已被禁止连接，请在服务端"授权管理"中解除」），已授权设备重复配对拒绝（Denied 设备提示禁止连接）；②认证请求：黑名单一律拒绝（即使表内权限未同步 Denied），表内 Denied 设备也拒绝；③业务阶段纵深防御：`isDenied(m_permission)` 拦截。**并发保护**：`m_pairingPending` 标志同一时刻只放行一个配对弹窗，其他并发配对请求暂时拒绝（不拉黑，提示稍后重试）。业务帧加解密分发（AAD=id）、只读权限拦截写操作（isWriteCommand）、id 递增防重放；`withProjectContext` 模板内切绑定项目→执行→恢复。设备管理（**2026-08-06 重写，黑名单与表合并**）：`listAuthorizedDevices`——表记录 + 黑名单合并（表内记录若在黑名单中权限统一视为 Denied；仅黑名单无表记录的合成条目 `ignoredOnly=true`、名称「已拒绝设备·uuid前8位」；排序：正常授权在前按授权时间倒序、黑名单在后）；`deleteAuthorizedDevice` = 撤销授权 + **移出黑名单（解除禁止）** + 断开在线连接；`updateDevicePermission` = 先同步黑名单（Denied 加入/否则移出）再更新表记录（纯黑名单无记录设备跳过 DB），改 Denied 时立即断开在线连接；`renameAuthorizedDevice`；`disconnectDevice(uuid)` 按 `RemoteConnection::deviceId()` 断开该设备全部连接（由 onDisconnected 统一清理）。内部类 `RemoteConnection`：收帧解析、`deviceId()`（认证/配对成功绑定）、`disconnectRemote()`（主动断开触发统一清理）、静默 5s 检查/60s 判定断线。`static localAddresses()` 仅枚举 `192.168.0.0/16` 私有段 IPv4。信号：`stateChanged/connectionCountChanged/clientConnected/clientDisconnected/deviceListChanged`。importPdf 落临时目录后走 DrawingService；getDrawingFile 读文件 base64 回传。删除请求 = 服务端物理执行。**2026-08-10 写操作幂等去重**：`handleBusiness` 写命令入口（权限拦截后）取请求内 `idempotencyKey` → 查 remote_idempotency 表（RemoteServer::getIdempotencyResult/insertIdempotencyResult，withProjectContext 包装）——命中则原样返回首次响应（含成功/失败结果）不重复执行；未命中执行后记录首次响应。**防超时重试重复创建（幂等键）**。
- **remoteclient.h/.cpp**：客户端。`connectTo(address)` TCP+握手（`kConnectTimeoutMs=60000`）——**有凭证走 auth（doAuth：挑战-响应），无凭证/认证失败回退配对（doPair：生成 uuid/pin/key 发 connect）**，成功保存凭证并建加密会话；**认证失败消息含「禁止连接」→ 不清凭证、不回退配对（重试同样被拒）**。连接流程/控制帧保持同步（一次性操作、模态等待可接受）。**2026-08-10 业务 API 全面异步化**：22 个 `xxxAsync` 方法（返回 `reqId`）经 `sendRequestAsync` 发加密请求并登记 `m_pendingBusiness`（QHash<reqId, PendingReq{cmd}>）；`readyRead` 分流——id 在 m_pendingBusiness 走 `dispatchBusinessResponse`（解密→`requestFinished(reqId,ok,data,err)` 信号；**getDrawingFile 特殊处理：base64 写 m_tempDir、data 改放 tempFilePath**），匹配 m_pendingId 的控制帧响应仍走 `responseReady`（握手）；断线（disconnected 信号）清理全部 pending 并逐个发 requestFinished 失败。**旧同步业务 API 保留为薄桥接**（`sendRequest` 调 sendRequestAsync + QEventLoop 等 requestFinished + 5s 超时查 pending 判定），调用点迁移完毕后删除。**`awaitOnce(client,reqId,ctx,lambda)` 模板 helper（remoteclient.h 末尾）**：连接 requestFinished、匹配 reqId 后自动 disconnect，是全部异步调用的统一模式。写命令自动附幂等键（computeIdempotencyKey）。失败路径（未连接/加密失败）用 `QTimer::singleShot(0,...)` 延迟 emit requestFinished（让调用方先 connect awaitOnce）。心跳 15s（fire-and-forget）/静默 60s 断线不变；`abortConnecting()` 中止机制不变。

### ui/（25 对）
- **nodetablemodel.h/.cpp**：`QAbstractTableModel`。`struct DirectoryItem{Kind{Node,Drawing}; node; drawing; fullPartNo; pathHint; quantity=-1; partWithoutDrawing;}`。列：名称/类型/图号/数量/修改时间（`ColCount=5`，列宽记忆按此枚举）。`setItems/setFilterText/itemAt/nodeAt/drawingAt/sort`；`QCollator(Chinese)+setNumericMode(true)`（图号按数值、名称按拼音）；quantity -1 排后；当前版本加粗；零件无图注「（无图）」。
- **directorynavigator.h/.cpp**：后退/前进/上级/跳转，维护各 Tab 导航栈与标题。持 NodeService*、TabManager*。
- **directoryassembler.h/.cpp**：`assembleDirectoryItems`（Directory 取子节点+零件时图纸）、`assembleSearchResults`(递归+pathHint)、`fillItemAttributes`。**无回收站分支**（头注释仍残留「目录/回收站页」措辞，功能已无）。
- **clipboard.h/.cpp**：`NodeClipboard{Mode{None,Copy,Cut}; nodes}`；`pasteNodeClipboard`(Cut→move；Copy→`copyWithConflictResolution` 预检→冲突弹窗循环→copyNode)。辅助 `partNoPrefixFor`/`partNoConflicts`。
- **contextmenus.h/.cpp**：`ContextMenus{newMenu,nodeMenu,drawingMenu}` 三菜单（**无回收站菜单**），nodeMenu 内嵌「新建」子菜单。
- **dialogs.h/.cpp**：模态对话框：复制冲突(可编辑段号实时回写)、新建零件(含实时完整图号预览+可选PDF)、新建项目、新建部件、打开项目(QSettings lastProjectDir)、备份目标、关于(版本0.2)、属性。
- **deleteprogressdialog.h/.cpp**：删除进度弹窗——模态展示删除过程（进度条 + 逐项日志 + 顶部统计摘要 + 状态标签）；`setTotal(nodeCount,drawingCount,fileCount)`、`setCurrentText`、`addLog`(自动滚动)、`advance`、`setFailed`（状态标签显示原因、启用关闭按钮）、`finish`；**删除进行中关闭按钮禁用**，完成/失败后启用，`exec()` 等用户点关闭。
- **pdftabviewer.h/.cpp**：自包含 PDF 查看页。**Qt 6.11**：`setZoomMode(QPdfView::ZoomMode::FitInView)`(枚举类)、`pageNavigator()` 在 QPdfView 上、`jump(page,point,zoom)`、`isLoaded()` 查 `QPdfDocument::Status::Ready`。
- **pdfpreviewdialog / parteditordialog**：PDF 速览模态窗 / 本地零件编辑（属性+图纸表+导入/更新；图纸删除选项在 drawingoperations/mainwindow 层：KeepVersions/Renumber/取消）。
- **remoteparteditordialog.h/.cpp**：**远程零件编辑对话框**——远程标签双击零件打开，与本地零件编辑器对齐（属性 + 完整图纸区：版本列表/当前标记/预览/导出/删除/设为当前/导入新图纸）。**2026-08-10 全面异步重写**：构造 buildUi（空数据，名称/图号用 m_node）+ 并行 loadPartAsync + computeFullPartNoAsync，回调回填材质/数量/图号前缀并刷新图纸列表；`refreshDrawingList`（listDirAsync）与 onPreview/onExport（fetchDrawingFileAsync）、onImportDrawing（importPdfAsync）、onSetCurrent（setCurrentDrawingAsync）、onDeleteDrawing（deleteDrawingAsync）全部 awaitOnce；onAccept 串行 rename→updatePartNo→updatePartAttributes（std::function 嵌套持所有权）→ accept。`showRemotePartEditorDialog` 仍同步 bool（dialog.exec 模态事件循环处理回调），但构造不再阻塞 UI 线程。**头注释仍写「软删除进服务端回收站」，功能实为物理删除。**
- **pairingdialog.h/.cpp**（v2 新增，**2026-08-06 重写**）：**首次配对确认对话框（服务端）**——首行「新的连接请求待批准」+ 来源信息（客户端名称/IP）+ 大号口令（共享 `ui/pinlabel`：#39c5bb 40pt 粗体 + 红色下划线）；按钮固定两个：**「允许受限的访问请求」（授权但仅只读，后续可在设备管理中提升）** / **「拒绝连接且不再提示（N）」**（N=60s 倒计时，点击或倒计时归零即拒绝并拉黑该设备，`PairingResult.neverAskAgain` → `AppConfig::addIgnoredDevice`）；**窗口 X 关闭不算拉黑**。弹窗经 server 注册为「当前挂起弹窗」，客户端 `cancelPair` 可外部关闭（关闭视为拒绝且不记录）。权限选择已移除（2026-08-06 起固定只读）。经 `RemoteServer::setPairingResolver` 挂到 MainWindow；测试环境用自动批准 resolver 替代（无 GUI）。
- **pinlabel.h/.cpp**（v2 新增）：**共享口令自绘控件**——40pt 粗体 #39c5bb + 红色下划线，pairingdialog（服务端确认）与 pinpromptdialog（客户端展示）共用同一视觉。
- **pinpromptdialog.h/.cpp**（v2 新增）：**客户端配对口令展示窗口**——TCP 连通后立即弹出（与服务端确认弹窗并行）；首行「你的口令，剩余有效时间N秒」每秒自动刷新，第二行 PinLabel 显示 4 位口令；底部「取消」按钮；60s 倒计时结束自动取消（口令过期）；取消/过期发 `cancelRequested()` → 连接流程发 `cancelPair` 通知服务端关闭挂起弹窗，**双方均不记录任何配对结果**。以 WindowModal 顶层窗口非阻塞显示（可交互且不被连接对话框的模态循环禁用）。
- **devicemanagerdialog.h/.cpp**（v2 新增，**2026-08-06 重写**）：**授权管理对话框（服务端）**——网络菜单「授权管理(&M)…」打开（开放状态可用，未开放提示先开放）；4 列：设备名称/权限（**QComboBox 下拉：受限的访问权限/完全访问权限/不允许的连接**，更改即时生效，Admin 旧记录映射为完全访问）/首次授权/上次连接；顶部操作栏：刷新/重命名/删除设备；**仅黑名单设备（ignoredOnly，名称灰色显示）禁用重命名，删除=解除禁止**（纯黑名单设备确认文案「确定解除对设备…的禁止连接吗？」；解除后需客户端重新配对）；选中行变化刷新按钮可用状态；`deviceListChanged` 用 **QueuedConnection** 刷新（避免权限下拉框信号处理栈内重建表格导致控件提前销毁）；删除设备同时断开在线连接。
- **recentprojectsmenu.h/.cpp**：最近项目列表（QWidgetAction 行：按钮+✕），`elidedText(ElideMiddle,320)`。
- **sessionmanager.h/.cpp**：`hfadm.session`(INI,QSettings，程序同目录)，`SessionTab{TabType,projectPath,currentNodeId,pdfFilePath}`，上限 kMaxSessionTabs=32；**远程标签不保存**；只管理自己的键（tab*/activeIndex/count），不 clear 整个文件。**2026-08-06 新增列宽记忆**：`saveColumnWidths/loadColumnWidths`（键 `detailView/count + detailView/colN`，防御非法宽度 0<w≤4096 跳过；保存前只 remove「detailView」组，不影响其他键）。
- **tabmanager.h/.cpp**：`enum TabType{Directory,Pdf,Remote}`（**无 RecycleBin**）；`TabData{...,remoteClient*}`（远程目录/远程PDF 标签非空）。`openDirectoryTab/openPdfTab`(失败不建页,可带 remoteClient)/`openRemoteTab`(页面空 QWidget，数据由 MainWindow 经 RemoteClient 填 model)/`closeTab`(手动删 pdfViewer 与 TabData)/`closeAll/hasTabOfType/indexOfType/...`。
- **browsertabbar / browsertabwidget**：Firefox 风标签栏（顶部项目色带/滚轮切换/中键关闭/拖拽排序）；BrowserTabWidget 构造 setTabBar。
- **welcomepage.h/.cpp**：启动覆盖层，3 QToolButton（创建/打开/**连接远程**，图标上文字下），半透明磨砂。
- **nodeoperations / drawingoperations**：节点/图纸操作封装（对话框+确认→Service）。drawingoperations：`importPdfForPart`、`setCurrentVersionForPart`、`deleteDrawingWithConfirm`（物理删除确认弹窗）。
- **remotedialog.h/.cpp**（**2026-08-06 重构为状态机对话框**）：`showRemoteConnectDialog(parent, RemoteClient**)`——①「请输入目标地址」，仅「连接」按钮；地址非法 IPv4 清空输入框、占位提示变「输入内容错误」②「正在尝试连接」按钮变「取消」可中止；首次配对（TCP 已连通）立即弹出客户端口令窗口（PinPromptDialog）与服务端确认并行③失败「连接错误」保留输入可重试④成功关闭对话框返回 client（所有权转移）。仅接受 192.168.0.0/16 段（`isPrivateIPv4`），端口固定 312。
- **remoteoperations.h/.cpp**：远程操作辅助，对话框复用本地组件（dialogs），数据读写全走 RemoteClient。`remoteLoadDirectory`(搜索框非空走 search)、`remoteRenameNode`、`remoteDeleteNodes`（确认弹窗；**服务端物理删除**）、`remoteShowProperties`、`remotePasteClipboard`(Cut=move；Copy=冲突弹窗循环，镜像 clipboard.cpp 逻辑)、`remoteImportPdf`、`remoteSetCurrentDrawing`、`remoteDeleteDrawing`（**物理删除**）、`remoteOpenDrawing`（fetchDrawingFile 落临时文件供 PDF 标签）。**2026-08-10 起大部分函数已被 mainwindow 内联异步版替代（仅 remotePasteClipboard 的复制冲突循环仍在用）**；这些同步函数未删（内部走 sendRequest 桥接，可编译），待复制冲突循环异步化后统一清理。**头注释仍写「递归移入回收站」「软删除进回收站」，功能实为物理删除。**
- ~~recycleoperations~~（已删除）；~~remotebrowserwidget.h~~（废弃草稿，已于 2026-08-05 移除）。

### tests（本地测试，git 已忽略）
- **tests/test_core.cpp**：核心非 GUI 测试（图号规则/零件限制/图纸导入版本/物理删除链路），断言 pass/fail。覆盖：图号拼接/段唯一/子部件不继承/同父唯一/移动冲突/图纸自动命名 A→B/非PDF拒绝/复制换号/删当前交接/KeepVersions 删 B 留 A、C/Renumber 删 C 前移/收集删除计划（节点/图纸/文件统计）/物理删除子树级联。**71 通过 0 失败**。
- **tests/test_remote.cpp**：远程协议端到端（回环 127.0.0.1，v2）：开放 312→**首次配对（自动批准 resolver，验证口令/权限/会话建立）→ 加密业务流（createComponent/Part→图号占用/格式/完整→loadPart 属性→rename→updatePartAttributes→getPath→search→deleteNode）→ 断开 → 二次认证重连（凭证复用，挑战-响应验证）→ 中止连接（abortConnecting）**；**failures=0**；结果落盘 `remote_test_result.txt`（Windows 控制台日志可能被吞）。
- ~~scripts/deploy.bat~~（发布脚本，2026-08-05 已删除，不再同步；发布步骤见第 2 节）。

## 7. 关键业务规则

- **图号体系**：机型=名称；部件=机型名`.`段(0–9999、全机型唯一)；零件=父完整图号`.`段(同父唯一)。子部件不继承父段；机型改名后动态刷新。
- **物理删除（2026-08-05 改造，回收站移除）**：node/drawing 表 `deleted` 字段**保留但恒 0**（不再软删置 1，兼容旧软删残留，查询过滤 deleted=0）；删除 = 节点 + 全部子级 + 零件/部件 + 图纸记录 + 磁盘文件，**不可恢复**。删除前确认弹窗带统计（节点数/图纸数，含「此操作不可恢复」提示）；删除中模态进度弹窗（进度条 + 逐项日志，关闭按钮完成/失败后才启用）。多选删除合并统计去重；机型根节点不可删除。
- **图纸版本**：一零件多版本仅一个 is_current；导入自动取下一字母；`removeDrawing(KeepVersions)` 仅删、字母空缺保留，`(Renumber)` 前移+重命名磁盘文件；删当前且有剩余→最新设当前。
- **多项目共存**：同一 DatabaseManager 复用，切标签重开指向对应库；色带区分。
- **会话持久化**：`hfadm.session` 记录标签与**详情列宽（detailView 组）**，标签上限 32；远程标签跳过；启动恢复失败/空则显欢迎页。恢复顺序：restoreSession → restoreColumnWidths（默认 {320,120,200,90,170}）。
- **备份**：`wal_checkpoint(TRUNCATE)` 后复制 db+files 到时间戳目录。
- **远程访问（v2）**：一次开放一个机型（绑当前标签页 projectPath）。**首次配对**：客户端生成 UUID（`AppConfig::deviceUuid()` 持久化复用）+4位口令+16字节AES密钥发 connect → 服务端弹窗（固定只读「允许受限的访问请求」/「拒绝连接且不再提示（60）」60s 倒计时）→ 允许则存 remote_device 表建加密会话，拒绝且不再提示则拉黑（黑名单优先，重试直接拒）；**并发配对保护**：同一时刻只放行一个配对弹窗。**二次连接**：客户端按地址从 hfadm.session 读凭证发 auth → 服务端查设备发挑战 nonce → 客户端用密钥加密回 authResp → 验证通过建会话（失败清凭证回退配对；**「禁止连接」不清凭证直接失败**）。**权限**：受限（只读，拦截写操作）/完全（可写）/不允许（黑名单，配对/认证/业务三层拒绝）；Admin 旧记录等价完全。**设备管理（授权管理）**：列表=表记录+黑名单合成（黑名单为行为真相源）；改「不允许」=拉黑+断线，解除=移出黑名单（纯黑名单设备需重新配对）；删除=撤销授权+解除禁止+断线。**业务帧全程 AES-128-GCM 加密（AAD 绑定帧 id 防重放）**；写操作前切绑定项目上下文、完事恢复；**写操作幂等去重（2026-08-10：内容哈希键 + remote_idempotency 表，超时重试返回首次响应不重复执行）**；**客户端业务 API 全面异步化（2026-08-10：22 个 xxxAsync + requestFinished + awaitOnce，UI 不再阻塞；连接/握手仍同步；旧同步 API 桥接保留待迁移完毕删除）**；**服务端删除节点/图纸均为物理执行**；客户端仅限 192.168.0.0/16；连接/握手 60s 超时（与口令有效期一致）；心跳 15s/静默 60s 断线；远程标签写后不主动广播刷新（手动刷新可见）；关闭被开放机型标签自动停止开放；断线自动清理全部远程标签；剪贴板跨域（本地↔远程）互拒；远程无回收站/备份等本地功能。**搜索支持名称/图号/材质三关键词（本地+远程一致）**；**列表零件图号单元格可点击 → 系统默认 PDF 程序打开最新版图纸（临时缓存，程序退出清理）**。

## 8. 代码规范与约定

- 命名：类/枚举大驼峰；函数/变量小驼峰；成员 `m_` 前缀；全量中文注释。
- UI 按职责拆 25 文件；单文件不设行数硬限（2026-08-05 取消 ≤1000 行约定，以职责单一为准）。
- 数据/UI 分离：model 纯结构；DirectoryItem+NodeTableModel 解耦；directoryassembler 装配展示项。
- Qt 6.11 特性：QPdfView::ZoomMode 枚举类、QPdfPageNavigator、代码创建菜单栏、QCollator 拼音+数值排序、WAL/checkpoint、会话 QSettings、QSS 资源内嵌图标、QComboBox 单元格下拉（QTableWidget::setCellWidget）。

## 9. 已知注意点 / 遗留

- `model/component.h`、`model/deletionplan.h` 未列入主 CMakeLists 源清单（均 header-only 无碍，属疏漏）。
- **注释滞后残留（功能已物理删除，注释未同步）**：`ui/directoryassembler.h`「目录/回收站页」、`ui/remoteoperations.h`「递归移入回收站」「软删除进回收站」、`ui/remoteparteditordialog.h`「软删除进服务端回收站」。
- **黑名单双写一致性**：`remote/ignoredDevices`（hfadm.session）为行为真相源，`remote_device.permission=Denied` 只是同步镜像；列表以黑名单为准合并（表内权限被强制视为 Denied），改权限失败时可能短暂不一致，认证层以黑名单为准兜底。
- `node/drawing` 表 `deleted` 字段保留恒 0（兼容旧软删残留；`collectDeletionPlan` 子节点查询不过滤 deleted 保证删除彻底）。
- databasemanager.cpp ≈1100 行（建表+递归 SQL 集中，职责单一）。
- 搜索时图纸结果不匹配节点下图纸（drawings 恒空，按需求设计，非 bug）。
- 遗留：PDF 内容搜索未实现、自动备份策略未做、安装包未做；**复制粘贴「复制」冲突循环异步化未做**（remoteCopyWithConflictResolution 多层循环+弹窗+串行远程，仍走旧同步桥接）；**图号点击打开图纸缓存为程序退出清理**（未做"阅读器关闭即清理"的精确监控，需平台 API 拿阅读器进程句柄）；旧同步业务 API 桥接保留（调用点迁移完毕后删）。
- 构建/环境坑：①构建与运行测试 PATH 需含 `C:/Qt/Tools/mingw1310_64/bin` + `C:/Qt/6.11.1/mingw_64/bin`（windres/DLL），否则 `windres: fatal: CreateProcess failed`；②主程序 configure 必须用编译器绝对路径；③`icon.rc.in` 被误删会导致 CMake configure_file 失败（git checkout 恢复）；④空 QStringList 写 INI 变 `@Invalid()`，清空用 `settings.remove`；⑤`RemoteServer::stop()` 勿先 delete m_server（socket 悬空崩溃），socket 生命周期由 setParent 接管；⑥单标签会话恢复 `setCurrentIndex` 不触发 currentChanged → 需显式 `activateProjectContext`；⑦`deviceListChanged` 刷新用 QueuedConnection（避免下拉框交互中被重建销毁）。
- 环境注意：本机 `rm` 被 safe-delete 拦截（中文路径+/c/ 路径致回收站工具失败）；**批量删除（>50 文件）时 WorkBuddy safe-delete 强制走回收站且本机回收站 API 报错（genie-trash "Some operations were aborted" / COM "系统不支持该功能"），bash rm、PowerShell Remove-Item、cmd rd 均被拦；用 PowerShell .NET 原生 API `[System.IO.Directory]::Delete(path, $true)` 可成功删除**。GUI 程序 EXITED 0 多正常，进程存活才是权威；验证「关闭保存」用 PowerShell `CloseMainWindow()`（WM_CLOSE 触发 closeEvent，taskkill /F 不触发）。QTemporaryDir/QElapsedTimer 非 QObject 不能带 parent；moc 需要 NodeTableModel/TabManager 实现进测试目标。windeployqt 路径须 Windows 风格 `C:/...`（bash 的 /c/ 会被拒）。
